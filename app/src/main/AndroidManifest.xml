<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ozsaat.reader">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.reader.ReaderAppActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.web.WebActivity"
            android:label="@string/title_activity_web" />
        <activity
            android:name=".ui.reader.BlogReaderActivity"
            android:label="@string/title_activity_blog_reader" />
        <activity
            android:name=".ui.NavDrawerActivity"
            android:label="@string/title_activity_nav_drawer" />
        <activity
            android:name=".rss.ListActivity.MySimpleArrayAdapter"
            android:label="@string/title_activity_my_simple_array_adapter"></activity>
        <activity
            android:name=".ListActivity.MyListActivity"
            android:label="@string/title_activity_my_list"></activity>
    </application>

</manifest>
